<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Painel de Controle</title>
</head>
<body>
  <h2>Bem-vindo, <%= user.username %>!</h2>
  <p>Você está logado como <%= user.role === 'admin' ? 'Administrador' : 'Funcionário' %>.</p>

  <% if (user.role === 'admin') { %>
    <a href="/register-funcionario">Cadastrar Funcionário</a>
  <% } %>

  <a href="/logout">Sair</a>
</body>
</html>
